# 低多边形卡丁车赛车游戏开发计划

## 1. 目标与范围

构建一个运行在现代桌面 Chrome 的 HTML5 低多边形 3D 卡丁车游戏，技术栈为 **Vite + TypeScript + Three.js**，并使用 **pnpm** 管理依赖、**Playwright** 进行端到端测试。

首个可交付版本包含：

- 第三人称跟随视角、键盘和鼠标菜单操作。
- 阳光明媚草原主题：程序化低多边形赛道、草地、树木、岩石、花丛、围栏与起终点拱门。
- 一条含直道、缓弯、急弯的闭环赛道。
- 街机式驾驶：加速、刹车/倒车、转向、手刹漂移、碰撞和基础粒子反馈。
- 车辆赛道限制：护栏阻挡；草地不降低最高速度，仅生成扬尘与越野音效反馈；按 `R` 重置到最近的合法赛道位置。
- 两种游戏模式：
  - **个人计时赛**：3 圈，记录总时间和最佳单圈，使用 `localStorage` 持久化。
  - **多人比赛**：玩家加 3 个 AI 对手，3 圈、倒计时起跑、实时排名和完赛结算。
- 基础音效：引擎、倒计时、碰撞/越野减速、漂移，以及驶入草地的扬尘反馈。
- Playwright 端到端测试，验证核心菜单、模式、驾驶约束、结算及持久化流程。

不纳入首版范围：移动端触摸操控、在线多人、外部 3D 模型/贴图、复杂车辆损伤、道具系统和多赛道选择。

## 2. 技术方案

| 范畴 | 方案 |
| --- | --- |
| 构建与包管理 | Vite、TypeScript、pnpm |
| 渲染 | Three.js，WebGLRenderer，阴影、雾、天空色与方向光 |
| 游戏循环 | `requestAnimationFrame`，以 `deltaTime` 更新输入、赛车、AI、比赛状态、相机、粒子与 HUD |
| 赛道数据 | 闭环 Catmull-Rom 曲线及等距采样点；采样点包含位置、前进方向、横向、累计距离等信息 |
| 车辆物理 | 自定义轻量街机运动模型，不引入重量级物理引擎 |
| AI | 基于赛道采样点的前视目标转向；不同目标速度、转向误差和橡皮筋修正 |
| 音频 | Web Audio API 生成或管理简单音效；首次用户交互后恢复音频上下文 |
| UI | DOM/HUD 覆盖层；中文文案，保证 Playwright 可通过语义 role 或 `data-testid` 定位 |
| 测试 | Playwright，启动 Vite 预览/开发服务器后执行 Chromium 测试 |

## 3. 项目结构

```text
.
├── index.html
├── package.json
├── pnpm-lock.yaml
├── tsconfig.json
├── vite.config.ts
├── playwright.config.ts
├── src/
│   ├── main.ts
│   ├── styles.css
│   ├── game/
│   │   ├── Game.ts                 # 生命周期、主循环、模式切换
│   │   ├── GameState.ts            # 菜单、倒计时、比赛、结算等状态
│   │   ├── constants.ts
│   │   ├── track/
│   │   │   ├── Track.ts            # 曲线、采样、道路网格、赛道查询
│   │   │   └── Environment.ts      # 草地与程序化低多边形场景
│   │   ├── kart/
│   │   │   ├── Kart.ts             # 赛车视觉对象和街机运动模型
│   │   │   ├── PlayerKart.ts       # 键盘输入接入
│   │   │   └── AiKart.ts           # 路径跟随与速度策略
│   │   ├── systems/
│   │   │   ├── InputSystem.ts
│   │   │   ├── RaceSystem.ts       # 圈数、检查点、名次、终局
│   │   │   ├── CollisionSystem.ts  # 护栏、草地、车辆间基础碰撞
│   │   │   ├── CameraSystem.ts
│   │   │   ├── AudioSystem.ts
│   │   │   └── ParticleSystem.ts
│   │   └── ui/
│   │       ├── Menu.ts
│   │       ├── Hud.ts
│   │       └── Results.ts
│   └── storage/
│       └── TimeTrialRecords.ts
└── tests/
    ├── helpers.ts
    ├── menu.spec.ts
    ├── time-trial.spec.ts
    ├── race.spec.ts
    └── driving-constraints.spec.ts
```

文件名称可根据实现中的自然边界微调，但应保持渲染、赛道查询、车辆控制、比赛规则、UI 和测试相互解耦。

## 4. 核心设计

### 4.1 赛道与场景

1. 用固定控制点创建闭环 `CatmullRomCurve3`，并均匀采样为赛道中心线。
2. 从每个采样点的切线计算横向向量，按固定半宽构建道路网格、内外草地区域和护栏位置。
3. 为每个采样点记录归一化进度与累计距离，统一服务于：
   - 赛车寻找最近赛道位置；
   - AI 的前视目标；
   - 检查点/圈数判定；
   - 越界判定和 `R` 重置；
   - 实时排名。
4. 使用基础几何体组成低多边形环境，采用固定种子或确定性分布，避免测试和调试时场景随机变化。
5. 使用天空背景、浅色雾、环境光、阳光方向光与阴影，营造晴朗草原风格；不依赖外部模型或贴图。

### 4.2 街机车辆与赛道限制

赛车状态至少包含位置、朝向、前进速度、横向滑移量、转向输入、当前赛道进度和圈数。

- `W`：加速；`S`：刹车/低速倒车；`A`/`D`：转向；`Space`：手刹漂移；`R`：重置。
- 转向幅度随速度插值，避免高速瞬间急转；车辆采用速度、阻力、抓地力和侧向滑移的简化模型，形成易掌握的漂移感。
- 在赛道可行驶区域内保持完整性能；轮胎驶入草地后不降低最高速度，仅生成扬尘与短暂的越野音效反馈，以保留街机式手感。
- 超过外侧边界时按护栏法线修正位置与速度，阻止赛车继续离开赛道；显示碰撞反馈并播放短音效。
- `R` 将玩家车重置到最近中心线附近，朝向赛道前进方向，速度归零或降至安全值。该功能同时用于无法自动恢复的异常状态。
- 赛车间采用简化圆形/胶囊近似碰撞，防止互相穿模且不引入复杂翻车物理。

### 4.3 比赛规则

游戏状态流转：`MENU -> COUNTDOWN -> RACING -> RESULTS`。

**个人计时赛**

- 玩家独自完成 3 圈。
- HUD 展示当前圈数、当前总时间、当前单圈时间和最佳单圈。
- 每次穿过起终点时，须先满足赛道进度要求，防止倒车或在起点附近刷圈。
- 完成第三圈后展示总时间、每圈成绩和个人最佳记录。
- 使用 `localStorage` 保存最佳总时间与最佳单圈；容错处理无效存储数据。

**多人比赛**

- 玩家和 3 个 AI 在不同起跑格出发，统一倒计时后允许加速。
- 赛事共 3 圈，HUD 展示圈数、排名、领先车手和必要的倒计时信息。
- AI 以赛道中心线前方采样点为目标，依据弯道曲率调整期望速度；每名 AI 有固定的小幅驾驶特征差异。
- 通过受限橡皮筋修正降低完全脱节概率，但不得瞬移或无视护栏。
- 完赛后按完成顺序、完成时间或赛道进度排列名次；玩家完成后进入结算，未完成 AI 以状态标记展示。

### 4.4 相机、音效和粒子

- 相机位于赛车后上方，使用平滑插值追踪位置和朝向；保留足够视野预判前方弯道。
- 速度较高时可轻微增大 FOV，碰撞时做短促、低幅的相机震动；效果应可在配置中关闭或调低。
- 音频系统基于用户首次点击开始按钮初始化，避免浏览器自动播放限制：
  - 引擎：随玩家速度、油门调整音量和音高；
  - 倒计时：短提示音；
  - 护栏碰撞、草地和漂移：短暂反馈音。
- 粒子系统使用对象池，避免每帧创建对象：
  - 草地与漂移时的扬尘；
  - 碰撞时少量碎屑/火花；
  - 限制最大粒子数并按寿命回收。

### 4.5 UI 与可访问测试接口

- 以中文提供主菜单：“个人计时赛”“多人比赛”“开始比赛”。
- 游戏中 HUD 包含模式、圈数、计时、名次与操作提示。
- 结算页显示成绩并提供“再来一局”和“返回主菜单”。
- 所有可点击控件均使用语义化 `button` 和明确可访问名称；关键状态区域添加稳定的 `data-testid`，例如：
  - `mode-menu`、`start-race`、`race-hud`、`lap-counter`、`position-counter`、`results-panel`。
- 不将游戏正确性仅依赖于渲染像素。开发环境中提供只读、无副作用的调试状态接口（例如 `window.__gameDebug`），仅暴露给测试所需的赛车、比赛和存储查询，以及可控的测试推进/重置能力。生产构建不应公开该接口。

## 5. 实施阶段

### 阶段 1：项目初始化与质量基线

1. 初始化 pnpm 项目、Vite 和 TypeScript。
2. 安装 `three`、`@types/three`、`@playwright/test` 等必要依赖。
3. 配置开发、构建、预览、测试和测试报告脚本。
4. 建立基础 DOM 页面、全屏 canvas 和 UI 样式变量。
5. 配置 Playwright 的 Chromium 项目、Web 服务器和失败截图/追踪保留策略。

**完成标准**：`pnpm dev` 可启动空场景，`pnpm build` 成功，Playwright smoke test 能加载主页。

### 阶段 2：Three.js 场景与草原赛道

1. 创建渲染器、场景、相机、光照、阴影和 resize 处理。
2. 实现闭环赛道的曲线采样与查询 API。
3. 生成道路、草地、护栏、起终点线与检查点可视化。
4. 以几何体搭建树木、岩石、花丛、围栏和起终点拱门。
5. 验证赛道查询可准确返回最近点、横向偏离、前进方向与进度。

**完成标准**：玩家可从第三人称视角观察完整、清晰的阳光草原闭环赛道。

### 阶段 3：玩家赛车与操控约束

1. 创建程序化低多边形卡丁车模型，包括车身、座椅、方向盘和可转动车轮。
2. 实现输入系统和街机运动模型。
3. 实现草地越野反馈（扬尘与音效）、护栏阻挡、基础车辆碰撞和 `R` 重置。
4. 实现平滑跟随相机、速度相关 FOV 和基础 HUD。
5. 增加可测试的调试状态读接口。

**完成标准**：玩家能完成稳定驾驶；不能穿过护栏离开赛道；草地仅产生扬尘与音效反馈、不降低最高速度；随时能恢复到最近赛道位置。

### 阶段 4：比赛系统与计时赛

1. 实现倒计时、起跑锁定、检查点判定、有效圈与 3 圈完成逻辑。
2. 实现计时器、单圈记录、结算面板和重开流程。
3. 实现 `localStorage` 的最佳总时间/最佳单圈读写与展示。
4. 处理重新开始、返回菜单和页面刷新后的状态恢复边界。

**完成标准**：玩家能够从菜单启动 3 圈计时赛，正确结算并在下次进入时看到已保存的记录。

### 阶段 5：AI 与多人比赛

1. 实现 AI 赛车及其赛道前视、转向和速度控制。
2. 配置 3 名具有轻微风格差异的 AI 对手。
3. 实现多车起跑格、比赛排名、进度比较和橡皮筋限制机制。
4. 实现多人赛 HUD、结算排序和再赛流程。
5. 在急弯、相撞和玩家脱离赛道时调校 AI，确保不会持续卡死或频繁跳变。

**完成标准**：四辆卡丁车可完成完整比赛；排名实时更新；玩家可获得稳定的最终名次。

### 阶段 6：反馈、打磨与测试

1. 实现 Web Audio 基础音效和音量管理。
2. 实现扬尘、漂移烟雾及碰撞粒子对象池。
3. 调校色彩、雾、阴影、UI 动画及性能预算，目标为现代桌面 Chrome 中稳定接近 60 FPS。
4. 编写并稳定 Playwright 测试；使用测试专用调试接口规避实时渲染、音频权限和 AI 随机性造成的脆弱断言。
5. 执行生产构建、浏览器回归和手动体验检查。

**完成标准**：核心流程的自动测试稳定通过，游戏具备完整的首版视觉和听觉反馈。

## 6. Playwright 验收计划

测试应在隔离浏览器上下文中运行，使用虚假或禁用音频输出，并对随机数、赛事参数和时间推进采用确定性测试模式。

| 测试 | 核心断言 |
| --- | --- |
| 菜单加载与模式选择 | 页面展示两个中文模式按钮；选择模式后按钮状态和启动文案正确。 |
| 计时赛启动 | 点击开始后出现倒计时和 HUD；倒计时完成前玩家输入不会带动车辆，完成后可加速。 |
| 键盘操控 | 按住 `W` 后玩家速度和赛道进度增加；`A`/`D` 改变车体朝向；`Space` 进入可观测的漂移状态。 |
| 赛道约束 | 将测试车辆置于草地时最高速度不被额外限制；将其推进护栏外侧后位置被修正回允许边界；按 `R` 重置至最近赛道点。 |
| 计时赛结算与持久化 | 通过测试推进完成 3 圈；显示结算；最佳总时间和最佳单圈写入 `localStorage`；重新载入后记录仍显示。 |
| 多人比赛 | 启动四车赛事，确认生成 3 个 AI 和排名 HUD；推进比赛完成后展示有序名次与玩家成绩。 |
| 回归流程 | 从结算页“再来一局”和“返回主菜单”均能进入正确状态，且不会遗留上一局的圈数或计时。 |

渲染测试只保留少量关键截图或 canvas 非空检查，功能正确性以 DOM、调试状态接口和确定性推进结果为主。

## 7. 开发与验证命令

计划提供以下 pnpm 脚本：

```bash
pnpm dev
pnpm build
pnpm preview
pnpm test:e2e
pnpm test:e2e:ui
```

开发阶段的验证顺序：

1. 每次改动先执行 `pnpm build`，确保 TypeScript 与生产打包通过。
2. 对改动对应的 Playwright spec 执行定向测试。
3. 阶段完成后执行完整 `pnpm test:e2e`。
4. 在 Chrome 中手动验证操控手感、AI 行为、粒子效果、音频解锁和 60 FPS 体验。

## 8. 风险与控制措施

| 风险 | 控制措施 |
| --- | --- |
| 赛道越界、圈数与排名规则互相不一致 | 所有系统共享同一份采样赛道进度数据，不分别推导赛道位置。 |
| AI 在急弯或碰撞后卡住 | 使用前视目标、弯道限速、最近赛道点校正与安全的异常重置。 |
| WebGL/实时循环导致 E2E 测试不稳定 | 引入确定性测试模式与窄范围调试接口，避免基于固定等待时间断言。 |
| 浏览器屏蔽自动播放音频 | 在“开始比赛”点击后初始化或恢复 AudioContext，并在无音频环境下优雅降级。 |
| 粒子、阴影和装饰物影响帧率 | 合并静态几何体、限制阴影投射对象、使用粒子对象池和最大数量上限。 |
| `localStorage` 数据损坏 | 读取时验证 JSON 和数值范围，异常时回退为空记录。 |

## 9. 首版验收清单

- [ ] 使用 pnpm 安装、开发、构建和运行测试。
- [ ] 游戏以中文菜单进入个人计时赛或多人比赛。
- [ ] 赛道、草原环境和赛车均由低多边形程序化几何体构成。
- [ ] 玩家支持约定的键盘操控及第三人称跟随视角。
- [ ] 赛车驶入草地仅产生扬尘与音效反馈（不降低最高速度），受护栏阻挡和 `R` 重置约束，无法持续驶离赛道。
- [ ] 计时赛准确运行 3 圈，保存并显示最佳总时间和最佳单圈。
- [ ] 多人比赛含 3 个 AI、3 圈、实时排名和结算结果。
- [ ] 具备基础音效、漂移/扬尘和碰撞粒子反馈。
- [ ] `pnpm build` 和 Playwright 全量 E2E 测试通过。
